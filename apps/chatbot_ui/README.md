# chatbot_ui

Streamlit frontend for the multi-LLM chat platform: provider/model picker,
streaming responses, and a sidebar listing saved chat sessions.

See the [project README](../../README.md) for setup instructions.

## Local dev

```bash
uv sync
uv run --package chatbot_ui streamlit run src/chatbot_ui/app.py
```
